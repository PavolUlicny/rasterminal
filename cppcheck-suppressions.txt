// Vendored headers are outside project analysis.
*:vendor/*

// cppcheck cannot cover every stb_image configuration.
toomanyconfigs

// Tests exercise these header utilities.
unusedFunction:src/terminal/framebuffer.h
unusedFunction:src/math/linalg.h

// Platform stubs make checks constant in individual configurations. Permit their inline
// suppressions to become unmatched if a later cppcheck version stops reporting them.
unmatchedSuppression:src/main.cpp

// The Windows shared-memory stub makes two framebuffer conditions appear constant.
unmatchedSuppression:src/terminal/framebuffer.cpp

// Header suppressions have no unmatched pair because headers are not primary inputs.

// Keep these explicit loops; some must remain constexpr.
useStlAlgorithm:src/loaders/mesh.cpp
useStlAlgorithm:src/loaders/mesh_obj.cpp
useStlAlgorithm:src/loaders/mesh_ply.cpp
useStlAlgorithm:src/platform/control.h
useStlAlgorithm:src/platform/terminal_io.h
